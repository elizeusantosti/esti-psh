BACKUP:

parametros:
-ajuda, -help, -disco, -exclusao, -destino, -verbose, -log

comportamento:
Se o parametro -disco não for especificado utiliza o disco padrão.
Se o arquivo de exclusao não for especificado utiliza o arquivo padrão

Se o arquivo log não for especificado gera um arquivo genérico.
Se o arquivo 

templates:
auto - 

exemplos:

script:
comum: esti backup (Utiliza o modo padrao e não possui parametros.)

modulo:
comum: backup auto (Usa o template auto)
mediano: backup padrao -log exemplo.txt
avançado: backup -disco c,d -destino f,e -exclusao lista.txt -verbose -log exemplo.txt
admin:
----------------------