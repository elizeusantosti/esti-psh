regex: $variable -match "\.(txt|xls|xlsx|doc|docx|pdf)"

function extensao {
    foreach ($arg in $args) {
        New-Variable -Name "$arg" -Value ($arquivos -match "\.($arg)")
        Get-Variable -Name $arg -ValueOnly
    }
}

